import { Element } from 'slate';

import ParagraphType from './ParagraphType';

interface ParagraphElementType extends Element {
    type: ParagraphType;
}

// eslint-disable-next-line no-undef
export default ParagraphElementType;
