export interface Stylable {
    bold?: boolean;
    italic?: boolean;
    subscript?: boolean;
    superscript?: boolean;
    underlined?: boolean;
    selection?: boolean;
    highlighted?: boolean;
}
